- Can the mirroring be smarter and idempotent by not removing the manifest folder?
- Stop using lvm storage and just use the minio test vm (?)
- Add some forcing of argo refresh to speed up mcg
- Create a way to just redeploy only the failed SNOs?
